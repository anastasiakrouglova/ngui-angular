import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Smart backpack',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
